class schedule {
  schedule({required this.id_schedule,required this.class_id,required this.clasrm_id,required this.group_id,required this.day,required this.time_first,required this.time_end});
  String id_schedule, class_id, clasrm_id, group_id, day, time_first, time_end;
}
